---
label: gal2
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: img
    type: file
    config:
      maxSize: 64
    label: img
  config:
    min: 
    max: 
    labelField: 
  label: images
- name: galeryname
  type: text
  config:
    required: false
  label: galeryname
